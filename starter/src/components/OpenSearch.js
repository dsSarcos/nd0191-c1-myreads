import "../App.css"
import { Link } from "react-router-dom";

const OpenSearch = () => {

    return (
        <div className="open-search">
            <Link to="/search" />
        </div>
    );
};

export default OpenSearch;